{% extends 'JordiLlonchCrudGeneratorBundle::layout.html.twig' %}

{% block title %}
    {{ parent() }} - {{ 'views.new.creation'|trans({'%entity%': 'Product'}, 'JordiLlonchCrudGeneratorBundle') }}
{% endblock %}

{% block page %}
    <div class="row">
        <div class="col-md-4">
            <h1>{{ 'views.new.creation'|trans({'%entity%': 'Product'}, 'JordiLlonchCrudGeneratorBundle') }}</h1>

            <form class="well" action="{{ path('product_create') }}" method="post" novalidate {{ form_enctype(form) }}>
                {{ form_widget(form) }}
                <p>
                    <button type="submit" class="btn btn-success">{{ 'views.new.create'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}</button>
                </p>
            </form>

            <div>
                <div class="float-left">
                    <a class="btn" href="{{ path('product') }}">
                        {{ 'views.recordactions.backtothelist'|trans({}, 'JordiLlonchCrudGeneratorBundle') }}
                    </a>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-8">
        <a href="" class="btn btn-info">Dodaj wariant</a>
        <a href="" class="btn btn-success">Dodaj opcje produktu</a>
        <a href="" class="btn btn-success">Utwórz warianty z opcji</a>
    </div>
{% endblock %}